name: Run Stock Algo

on:
  schedule:
    - cron: '0 13 * * 1-5'
  workflow_dispatch:

jobs:
  run-algo:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install yfinance vaderSentiment pyyaml pandas numpy

      - name: Run the algo
        run: python ALGO_STANDALONE.py

      - name: Commit report back to repo
        run: |
          git config user.name "stock-algo-bot"
          git config user.email "bot@github.com"
          git add outputs/ || true
          git diff --staged --quiet || git commit -m "Daily report $(date +'%Y-%m-%d')"
          git push || true
